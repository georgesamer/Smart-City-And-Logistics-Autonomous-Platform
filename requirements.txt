# Core runtime dependencies (FastAPI server)
fastapi>=0.100
uvicorn>=0.23

# Interactive dashboard (Streamlit Dashboard)
streamlit>=1.40

# Development and testing
pytest>=7.0
httpx>=0.24

# Note: optional deep-perception engine dependencies (torch/transformers)
# Listed separately in requirements-ml.txt
